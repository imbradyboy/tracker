import {Singleton} from './index.js';
const callback;

exports.meta = function (snapshot, tag, custom) {
    
    if (Singleton)
        return new promise((resolve, reject) => {

        })
    else {

    }
}

const setCallback = (func) => callback = func;
export default {callback}