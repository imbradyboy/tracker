// Need to import Firebase, access current auth
// Log Auth UID
// Log the reference path
// Log the size of the payload
// Log the custom tag with it.

exports.realtime = function (snapshot, tag) {
    return new promise((resolve, reject) => {
     })
}
exports.firestore = function (snapshot, tag) {
    return new promise((resolve, reject) => { })
}
exports.storage = function (snapshot, tag) {
    return new promise((resolve, reject) => { })
}
exports.meta = function (snapshot, tag) {
    return new promise((resolve, reject) => { })
}
