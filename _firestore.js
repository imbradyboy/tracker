import {Singleton} from './index.js';
const callback;

exports.firestore = function (snapshot, tag, custom) {
    
    if (singleton)
        return new promise((resolve, reject) => {

        })
    else {

    }
}

const setCallback = (func) => callback = func;
export default {callback}